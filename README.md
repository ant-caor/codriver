# Codriver

Where drivers can manage smart deliveries.

## Running on Android

Execute the following command:

```bash
    $ yarn run android
```

## Running on iOS

Execute the following command:

```bash
    $ yarn run ios
```
